pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'echo ""'
            }
        }
        stage('deploy') {
            echo 'deployed on prod'
        }
    }
}
